import { Component,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { ActivatedRoute,Router } from '@angular/router'; import { ApiService } from '../services/api.service'; import { OtDetail } from '../models';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<div *ngIf="detail as d"><div class="page-head"><div><h1>{{d.ot.otId}}</h1><p>{{d.ot.clienteId}} · {{d.ot.patente}} · {{d.ot.descripcion}}</p></div><button class="danger" (click)="remove()">Eliminar OT</button></div><div class="grid"><section class="card"><h2>Datos</h2><dl><dt>Total</dt><dd>{{d.ot.total | currency:'CLP':'symbol-narrow':'1.0-0'}}</dd><dt>Creada</dt><dd>{{d.ot.createdAt | date:'medium'}}</dd><dt>Actualizada</dt><dd>{{d.ot.updatedAt | date:'medium'}}</dd></dl></section><section class="card"><h2>Agregar ítem</h2><form class="form" (ngSubmit)="addItem()"><label>Concepto<input [(ngModel)]="item.concepto" name="concepto" required></label><label>Cantidad<input [(ngModel)]="item.cantidad" name="cantidad" type="number" min="0.01" step="0.01" required></label><label>Precio unitario<input [(ngModel)]="item.precioUnit" name="precio" type="number" min="0" step="1" required></label><button>Agregar</button></form></section></div><section class="card"><h2>Ítems</h2><table><thead><tr><th>Concepto</th><th>Cantidad</th><th>Precio</th><th>Subtotal</th><th></th></tr></thead><tbody><tr *ngFor="let x of d.items"><td>{{x.concepto}}</td><td>{{x.cantidad}}</td><td>{{x.precioUnit | currency:'CLP':'symbol-narrow':'1.0-0'}}</td><td>{{x.subtotal | currency:'CLP':'symbol-narrow':'1.0-0'}}</td><td><button class="danger small" (click)="deleteItem(x.itemId)">Quitar</button></td></tr></tbody></table></section><div class="grid"><section class="card"><h2>Eventos Kafka persistidos</h2><pre>{{d.events | json}}</pre></section><section class="card"><h2>Notificaciones RabbitMQ persistidas</h2><pre>{{d.notifications | json}}</pre></section></div></div>`})
export class OtDetailComponent implements OnInit {id='';detail?:OtDetail;item={concepto:'',cantidad:1,precioUnit:0};constructor(private route:ActivatedRoute,private api:ApiService,private router:Router){}ngOnInit(){this.id=this.route.snapshot.paramMap.get('id')!;this.load()}load(){this.api.ot(this.id).subscribe(v=>this.detail=v)}addItem(){this.api.addItem(this.id,this.item).subscribe(()=>{this.item={concepto:'',cantidad:1,precioUnit:0};this.load()})}deleteItem(itemId:number){this.api.deleteItem(this.id,itemId).subscribe(()=>this.load())}remove(){if(confirm(`¿Eliminar ${this.id}?`))this.api.deleteOt(this.id).subscribe(()=>this.router.navigate(['/ots']))}}
