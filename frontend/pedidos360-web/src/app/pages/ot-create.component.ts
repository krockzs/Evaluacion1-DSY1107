import { Component } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { ApiService } from '../services/api.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<h1>Nueva orden de trabajo</h1><form class="card form" (ngSubmit)="save()"><label>Cliente ID<input [(ngModel)]="model.clienteId" name="clienteId" required maxlength="20"></label><label>Patente<input [(ngModel)]="model.patente" name="patente" required maxlength="10"></label><label>Descripción<textarea [(ngModel)]="model.descripcion" name="descripcion" maxlength="200"></textarea></label><label>Total CLP<input [(ngModel)]="model.total" name="total" required type="number" min="0" step="1"></label><div class="error" *ngIf="error">{{error}}</div><button [disabled]="saving">{{saving?'Guardando…':'Crear OT'}}</button></form>`})
export class OtCreateComponent {model={clienteId:'',patente:'',descripcion:'',total:0};saving=false;error='';constructor(private api:ApiService,private router:Router){}save(){this.saving=true;this.api.createOt(this.model).subscribe({next:(v:any)=>this.router.navigate(['/ots',v.otId]),error:e=>{this.error=e.error?.error||e.message;this.saving=false}})}}
